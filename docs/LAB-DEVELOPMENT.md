# مرجع تنفيذ المختبر

آخر تحديث: 24 سبتمبر 2026. نقطة البداية لهذه الجولة: `4b1469f` على فرع `lab`.

## اتجاه العمل

نبدأ من أحدث المختبر؛ فهو يحمل التطوير المتراكم في المحتوى والتجربة والتسويق. نسخة نبوع القديمة مرجع انتقائي للهيكل والتوازن البصري، وليست نسخة نرجع إليها بالكامل. لا ننشر تغييرات المختبر على `main` تلقائيًا.

نحافظ على الاسم والعناوين الفنية والألوان والخدمات الاثنتي عشرة والمصادر وروابط القراءة والإفصاح التجاري. يتحسن الموقع عندما يفهم الزائر اختياره ويكمل رحلته بسهولة، وليس بمجرد زيادة العناصر أو التحذيرات.

## ما تغيّر في هذه الجولة ولماذا

- أصبح للكروت قالب واحد بدل آخر طبقتين متعارضتين. مساحة الملخص ثابتة. وطبقًا للتصحيح الأحدث، تبقى «صلته بالعربية» داخل التفاصيل فقط. حسّنا قراءة الدولة والدومين ورابط الدليل، ومساحة الضغط على التفاصيل.
- الرجوع من القراءة يستعيد البحث والتصنيف والخدمة الأصلية والدولة وعدد النتائج المعروضة، وكذلك اختيارات التعلّم ومسار التصفح والتفاصيل المفتوحة. تحفظ الذاكرة داخل جلسة التبويب، وتنتهي بعد أربع ساعات وفق نظام التنقل القائم.
- أضفنا ربطًا واضحًا بين زر التفاصيل والجزء الذي يفتحه، والرجوع إلى الزر عند الإغلاق بلوحة المفاتيح.
- نُقلت بيانات الدليل والتعلّم وخرائط الدول والقراءة كما هي إلى ملف مستقل. لم نُجدّد تواريخ التحقق أو نغيّر الأسعار أو الادعاءات أثناء إصلاح الواجهة.

## أين يعدّل المطوّر التالي؟

- `js/catalog-data.js`: بيانات الخدمات ومصادر التعلّم والتصنيفات والدول وروابط الأدلة. يُحمّل قبل `app.js`.
- `js/app.js`: العرض والتصفية والتفاصيل. `guideLinkMarkup` هو القالب المشترك لروابط القراءة.
- `js/discovery-memory.js`: حفظ واستعادة اختيارات صفحة الاكتشاف؛ يتعاون مع `js/smart-return.js` المسؤول عن التنقل.
- `css/app.css`: عدّل كتلة `Directory cards: one compact template` عند تغيير الكروت. لا تضف نسخة جديدة من القالب في نهاية الملف. ما زالت أقسام أخرى تحتوي تنسيقات قديمة؛ راجعها عند العمل على القسم نفسه.
- `js/commercial-links.js`: الربط التجاري والإفصاح القائمان. لم تتغير قواعدهما في هذه الجولة.

## التحقق الضروري

بعد تغيير العرض أو ذاكرة التنقل، جرّب: اختيار خدمة أو مجال، فتح تفاصيل، قراءة دليل، ثم «ارجع لمكانك». يجب أن يبقى الاختيار وعدد النتائج صحيحين. جرّب أيضًا البحث والعودة بزر المتصفح، وقائمة التعلّم. افحص صفًا فيه ملاحظة منشأ وآخر دونها، ونصًا طويلًا، وعرضًا ضيقًا. لا تختبر نماذج المراسلة بإرسال طلب حقيقي.

في هذه الجولة اجتازت البيانات مقارنة كاملة قبل الفصل وبعده: 77 اكتشافًا و36 مصدر تعلّم، مع خرائط الدول والأدلة. اجتازت ملفات JavaScript فحص الصياغة، وروابط ملفات الأدلة فحص وجود وجهاتها محليًا. نتيجة التحقق التفاعلي تُسجّل مع اكتمال النشر.

## اقتصاد التطوير

اقرأ موضع القرار والملفات المتأثرة فقط. نفّذ تغييرًا له أثر مفهوم، واختبر الرحلة التي قد يكسرها. لا تبدّل إطار العمل ولا تضف خدمة مدفوعة أو استدعاء ذكاء اصطناعي إلى البحث لمجرد توفر أداة أحدث. قوة الأداة تُستخدم في إزالة العوائق وصيانة نتيجة متماسكة.


## استكمال الجولة مع التطوير الموازي

دُمجت تحسينات المسار `vision-2026-09-24` حتى `624320b` بعد مراجعة الفروق. بقي الفرع الجانبي نفسه دون تعديل. شملت التحسينات وضوح أسماء المجالات، وتقليل أدوات التصفية المكررة، وتنسيق القراءة، والإصلاحات المتعلقة بالتفاصيل و«+ المزيد» وإزالة Quick Peek المتروك.

حُفظت «تسوّق وشراء» وبقية التسميات المنقحة؛ لا تُستعاد ألفاظ أقدم مع استعادة تخطيط أحدث. لم تُحوّل أمثلة دليل اللغة مثل «قبل أن تختار» و«ما راجعناه مؤخرًا» إلى عناوين معتمدة. إظهار العناوين الحالية بوضوح لا يحتاج تغيير معناها.

مصدر تصحيح «صلته بالعربية» هو طلب المستخدم اللاحق في المسار الموازي، المسترجع بتاريخ 24 سبتمبر 2026 الساعة 12:55 UTC، وتطبيقه في `b8d2282`. سجل الكود يثبت التنفيذ؛ اعتماد كل تفاصيل الفرع بصريًا غير مسترجع. الدمج هنا استجابة لطلب المستخدم الحالي إدخال التطويرات الأحدث ذات الصلة في استكمال هذه الجولة، وليس إعلانًا بأن كل اقتراح سابق صار قرارًا نهائيًا.

أضيف إصلاح مرتبط: زر «مسح البحث» يمسح العبارة فقط ويحتفظ باختيار المجال والخدمة. وزر «إلغاء الدولة» يلغي تصفية الدولة فقط. لا يجوز أن يصف الزر فعلًا أضيق مما ينفذه.
