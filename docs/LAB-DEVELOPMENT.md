# مرجع تنفيذ المختبر

آخر تحديث: 25 سبتمبر 2026. بدأت جولة التنفيذ من `4b1469f` على `lab`، واكتملت عند `96414033dffd3e187d8f73ed511933e90d9fa2c7` في 24 سبتمبر. اعتُمدت هذه النتيجة مرجعًا أول للهيكل والتجربة في 25 سبتمبر. تحديث التوثيق اللاحق لا يغير ملفات الموقع التشغيلية.

## اتجاه العمل

نبدأ من المختبر المعتمد؛ فهو المرجع الأول للهيكل وتنظيم الملفات والمكونات والتوازن البصري وتجربة الاكتشاف والتفاصيل والقراءة والعودة، إلى جانب المحتوى والوظائف المتراكمة. نسخة نبوع القديمة مرجع تاريخي للصفاء في مرحلته، دون أولوية هيكلية أو بصرية تلقائية. لا يُستعار منها حل إلا لحاجة محددة وبعد مقارنته بما تحقق في المختبر. لا ننشر تغييرات المختبر على `main` تلقائيًا، ولا نعتبر كل التزام تالٍ معتمدًا لمجرد حداثته.

نحافظ على الاسم والعناوين الفنية والألوان والخدمات الاثنتي عشرة والمصادر وروابط القراءة والإفصاح التجاري. يتحسن الموقع عندما يفهم الزائر اختياره ويكمل رحلته بسهولة، وليس بمجرد زيادة العناصر أو التحذيرات.

## ما تغيّر في هذه الجولة ولماذا

- أصبح للكروت قالب واحد بدل آخر طبقتين متعارضتين. مساحة الملخص ثابتة. وطبقًا للتصحيح الأحدث، تبقى «صلته بالعربية» داخل التفاصيل فقط. حسّنا قراءة الدولة والدومين ورابط الدليل، ومساحة الضغط على التفاصيل.
- الرجوع من القراءة يستعيد البحث والتصنيف والخدمة الأصلية والدولة وعدد النتائج المعروضة، وكذلك اختيارات التعلّم ومسار التصفح والتفاصيل المفتوحة. تحفظ الذاكرة داخل جلسة التبويب، وتنتهي بعد أربع ساعات وفق نظام التنقل القائم.
- أضفنا ربطًا واضحًا بين زر التفاصيل والجزء الذي يفتحه، والرجوع إلى الزر عند الإغلاق بلوحة المفاتيح.
- نُقلت بيانات الدليل والتعلّم وخرائط الدول والقراءة كما هي إلى ملف مستقل. لم نُجدّد تواريخ التحقق أو نغيّر الأسعار أو الادعاءات أثناء إصلاح الواجهة.

## أين يعدّل المطوّر التالي؟

- `js/catalog-data.js`: بيانات الخدمات ومصادر التعلّم والتصنيفات والدول وروابط الأدلة. يُحمّل قبل `app.js`.
- `js/app.js`: العرض والتصفية والتفاصيل. `guideLinkMarkup` هو القالب المشترك لروابط القراءة.
- `js/discovery-memory.js`: حفظ واستعادة اختيارات صفحة الاكتشاف؛ يتعاون مع `js/smart-return.js` المسؤول عن التنقل.
- `css/app.css`: عدّل كتلة `Directory cards: one compact template` عند تغيير الكروت. لا تضف نسخة جديدة من القالب في نهاية الملف. ما زالت أقسام أخرى تحتوي تنسيقات قديمة؛ راجعها عند العمل على القسم نفسه.
- `js/commercial-links.js`: الربط التجاري والإفصاح القائمان. لم تتغير قواعدهما في هذه الجولة.

## التحقق الضروري

بعد تغيير العرض أو ذاكرة التنقل، جرّب: اختيار خدمة أو مجال، فتح تفاصيل، قراءة دليل، ثم «ارجع لمكانك». يجب أن يبقى الاختيار وعدد النتائج صحيحين. جرّب أيضًا البحث والعودة بزر المتصفح، وقائمة التعلّم. افحص صفًا فيه ملاحظة منشأ وآخر دونها، ونصًا طويلًا، وعرضًا ضيقًا. لا تختبر نماذج المراسلة بإرسال طلب حقيقي.

في هذه الجولة اجتازت البيانات مقارنة كاملة قبل الفصل وبعده، ومقارنة أخرى بعد دمج أحدث المسار الجانبي: 77 اكتشافًا و36 مصدر تعلّم، مع خرائط الدول والأدلة. اجتازت ملفات JavaScript فحص الصياغة، وروابط ملفات الأدلة فحص وجود وجهاتها محليًا.

على نسخة Vercel المنشورة اختُبرت رحلة ChatGPT، وإظهار النتائج الأربع، والتفاصيل، والقراءة والرجوع. بقي الاختيار وعدد النتائج. ثم اختُبر البحث عن «فنار»: عادت العبارة والنتيجة وموضع الصفحة، ومسح البحث احتفظ باختيار ChatGPT. نجح رجوع المتصفح أيضًا. وفي التعلّم عاد البحث عن «ينفع» كما كان، وعادت نافذة تفاصيل «كرنك» مفتوحة عند الرجوع من دليلها. تساوت الكروت الستة في الارتفاع عند العرض المكتبي، ولم يظهر تجاوز أفقي أو خطأ JavaScript من الموقع في الرحلات المختبرة. لا يوجد اختبار مباشر على هاتف في هذه الجولة؛ تظل هذه حدود التحقق، وليست شهادة خلو الموقع كله من الأخطاء.

## اقتصاد التطوير

اقرأ موضع القرار والملفات المتأثرة فقط. نفّذ تغييرًا له أثر مفهوم، واختبر الرحلة التي قد يكسرها. لا تبدّل إطار العمل ولا تضف خدمة مدفوعة أو استدعاء ذكاء اصطناعي إلى البحث لمجرد توفر أداة أحدث. قوة الأداة تُستخدم في إزالة العوائق وصيانة نتيجة متماسكة.


## استكمال الجولة مع التطوير الموازي

دُمجت تحسينات المسار `vision-2026-09-24` حتى `624320b` بعد مراجعة الفروق. بقي الفرع الجانبي نفسه دون تعديل. شملت التحسينات وضوح أسماء المجالات، وتقليل أدوات التصفية المكررة، وتنسيق القراءة، والإصلاحات المتعلقة بالتفاصيل و«+ المزيد» وإزالة Quick Peek المتروك.

حُفظت «تسوّق وشراء» وبقية التسميات المنقحة؛ لا تُستعاد ألفاظ أقدم مع استعادة تخطيط أحدث. لم تُحوّل أمثلة دليل اللغة مثل «قبل أن تختار» و«ما راجعناه مؤخرًا» إلى عناوين معتمدة. إظهار العناوين الحالية بوضوح لا يحتاج تغيير معناها.

مصدر تصحيح «صلته بالعربية» هو طلب المستخدم اللاحق في المسار الموازي، المسترجع بتاريخ 24 سبتمبر 2026 الساعة 12:55 UTC، وتطبيقه في `b8d2282`. سجل الكود يثبت التنفيذ؛ لم يكن اعتماد كل تفاصيل الفرع بصريًا مسترجعًا عند الدمج. ثم اعتمد المستخدم الجولة المكتملة من المختبر مرجعًا أول للهيكل والتجربة في 25 سبتمبر؛ يخص هذا الاعتماد النتيجة المجمعة، ولا يحيي كل اقتراح تاريخي في الفرع الجانبي. الدمج هنا استجابة لطلب المستخدم الحالي إدخال التطويرات الأحدث ذات الصلة في استكمال هذه الجولة، وليس إعلانًا بأن كل اقتراح سابق صار قرارًا نهائيًا.

أضيف إصلاح مرتبط: زر «مسح البحث» يمسح العبارة فقط ويحتفظ باختيار المجال والخدمة. وزر «إلغاء الدولة» يلغي تصفية الدولة فقط. لا يجوز أن يصف الزر فعلًا أضيق مما ينفذه.

## مراجعة التصنيف والربط — 25 سبتمبر 2026

نقطة الاستئناف: انتهى طلب رابط المشاركة السابق، وبدأ الطلب الحالي من المختبر عند `8ab0ae0`. طلب المستخدم فحص علاقة الأداة المألوفة بالمجال والتصنيف الفرعي لكل عناصر الدليل، بعد ملاحظته اختفاء أنغامي من الصوت. هذه مراجعة اكتشاف وتصنيف، ولا تعيد التحقق من الأسعار أو خصائص المنتجات أو مصادرها.

- نُقل أنغامي من `culture/all` إلى `culture/podcasts`؛ فيظهر مع صوت وثمانية في «بودكاست وصوت» وعند اختيار Spotify. بقي نوعه «بث صوتي»، ولم تتحول المقارنة إلى ادعاء تطابق الوظائف بين الخدمات.
- أصبح مدخلا Amazon وJumia يبدآن من `shopping/all` مع استمرار التصفية بالخدمة المختارة. كان الأول يخفي Mumzworld والثاني يخفي السوق المفتوح. بقيت تصنيفات «متاجر متخصصة» و«بيع وشراء بين الناس» كما هي؛ لا نغيّر حقيقة العنصر لتوافق مسارًا ضيقًا.
- عند تضييق التصنيف الفرعي، يبقى اختيار الأداة المألوفة إذا كانت لها عناصر في التصنيف المطلوب. إذا لم توجد علاقة، يُلغى اختيار الأداة كما كان يحدث سابقًا. لا يعني توسيع مسار Amazon أو Jumia إظهار كل التسوق دون شرط العلاقة.
- اجتهاد تصنيفي ضمن المراجعة: نُقلت فريش من `tech/all` إلى `shopping/specialized` بناءً على وصفها المحفوظ كعلامة أجهزة منزلية ومنتجات مادية. لا تُضاف لها علاقة Amazon أو Jumia من عندنا.

الفحص الآلي شمل 77 عنصرًا و18 مسار مقارنة و12 اختيارًا ظاهرًا. كشف ثلاث حالات إخفاء في النسخة السابقة، واجتازتها النسخة المصححة. المقارنة الكاملة لبيانات العناصر أثبتت أن التغيير اقتصر على حقول التصنيف المذكورة؛ بقيت العناصر الـ77 ومصادر التعلم الـ36 والنصوص والأسعار وتواريخ المراجعة والمصادر وخرائط الدول والأدلة كما كانت.

لإعادة الفحص بعد تعديل البيانات: `node scripts/audit-taxonomy.cjs`. يكشف التصنيفات غير المعرفة، وتكرار المعرفات، والمدخل الظاهر بلا مسار، والعناصر التي يحجبها مسار المقارنة المرتبطة به. ينبه أيضًا إلى التصنيف العام `all` ليُراجع بشريًا. لا يدّعي الحكم على ملاءمة كل مقارنة أو تحديث معلومات المنتج.

بقيت أسماء المقارنة التي ليست مداخل اختيار ظاهرة، مثل Stripe وQuickBooks، كما هي؛ وجود الاسم داخل البطاقة لا يستلزم إنشاء زر جديد أو فرض تصنيف واحد على خدمات متعددة الاستخدام. بقي تصميم المختبر المعتمد والعناوين وذاكرة العودة ونسخة نبوع و`main` والمسار الجانبي دون تغيير في هذه الجولة.
